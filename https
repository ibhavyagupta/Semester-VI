<!-- Info Banner -->
<div class="wr-info-banner" role="region" aria-label="Disclaimer">
  <span class="wr-info-icon" aria-hidden="true">ℹ️</span>
  <p class="wr-info-text">
    These numbers are estimates based on the amounts you entered. We haven’t included any unforeseen costs in our calculations.
  </p>
</div>

<!-- Accordion Section -->
<div class="wr-accordion-container">
  <div class="wr-accordion">
    <button class="wr-accordion-toggle" aria-expanded="false" aria-controls="accordion-content-1">
      <span class="wr-accordion-icon" aria-hidden="true">+</span>
      <span class="wr-accordion-title">What does gross and net mean?</span>
    </button>
    <div id="accordion-content-1" class="wr-accordion-content" hidden>
      <p>
        Gross rental yield measures your rental income against the price of the property before expenses.
      </p>
      <p>
        Net rental yield (and net annual return) is what you're left with after expenses.
      </p>
    </div>
  </div>
</div>

/* Info banner left-alignment on medium and up */
@media screen and (min-width: 768px) {
  .wr-info-banner {
    align-self: flex-start;
    text-align: left;
    justify-content: flex-start;
  }
}

/* Accordion icon rotate on open */
.wr-accordion-toggle.active .wr-accordion-icon {
  transform: rotate(45deg); /* plus ➕ becomes X ✖️ */
}

/* Accordion expanded content styling */
.wr-accordion-content {
  padding-top: 12px;
  font-size: 16px;
  color: #4b4b4b;
  line-height: 1.5;
}

/* Improve spacing in expanded state */
.wr-accordion.active {
  padding-bottom: 16px;
}

/* Ensure icon stays on left with space */
.wr-accordion-toggle {
  display: flex;
  align-items: center;
  gap: 8px;
}
